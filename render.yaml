services:
  - type: web
    name: dsa-patterns
    env: node
    buildCommand: npm install && npm run build
    startCommand: serve -s build
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
      - key: NODE_ENV
        value: production
    healthCheckPath: /
    autoDeploy: true 