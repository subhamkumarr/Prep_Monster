services:
  - type: web
    name: dsa-patterns
    env: node
    buildCommand: yarn install && GENERATE_SOURCEMAP=false yarn build
    startCommand: serve -s build
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /
    autoDeploy: true 